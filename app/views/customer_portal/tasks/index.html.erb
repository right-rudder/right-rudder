<div class="lg:pl-72">
  <div class="p-10">
    <h1 class="font-bold text-5xl">All Tasks</h1>
  </div>

  <% @accounts.each do |account| %>
    <div class="p-10 pl-20">
      <h1 class="font-bold text-4xl"><%= account.name %></h1>
      <div id="tasks" class="min-w-full flex flex-col gap-5 mt-10">
        <% if  account.tasks_overdue.any? %>
          <div>
            <h2 class="text-red-500 font-semibold text-xl py-2">Overdue</h2>
            <%= render partial: "tasks/task", collection: account.tasks_overdue, locals: { source: "portal_index" } %>
          </div>
        <% end %>

        <% if account.tasks_due_today.any? %>
          <div>
            <h2 class="font-semibold text-xl py-2">Due today</h2>
            <%= render partial: "tasks/task", collection: account.tasks_due_today, locals: { source: "portal_index" } %>
          </div>
        <% end %>

        <% if account.tasks_due_tomorrow.any? %>
          <div>
            <h2 class="font-semibold text-xl py-2">Due tomorrow</h2>
            <%= render partial: "tasks/task", collection: account.tasks_due_tomorrow, locals: { source: "portal_index" } %>
          </div>
        <% end %>

        <% if account.tasks_due_later_this_week.any? %>
          <div>
            <h2 class="font-semibold text-xl py-2">Due later this week</h2>
            <%= render partial: "tasks/task", collection: account.tasks_due_later_this_week, locals: { source: "portal_index" } %>
          </div>
        <% end %>
        
        <% if account.tasks_due_next_week.any? %>
          <div>
            <h2 class="font-semibold text-xl py-2">Due next week</h2>
            <%= render partial: "tasks/task", collection: account.tasks_due_next_week, locals: { source: "portal_index" } %>
          </div>
        <% end %>
        
        <% if account.tasks_due_later.any? %>
          <div>
            <h2 class="font-semibold text-xl py-2">Due later</h2>
            <%= render partial: "tasks/task", collection: account.tasks_due_later, locals: { source: "portal_index" } %>
          </div>
        <% end %>

        <% if account.tasks_completed.any? %>
          <div>
            <h2 class="text-green-500 font-semibold text-xl py-2">Completed</h2>
            <%= render partial: "tasks/task", collection: account.tasks_completed, locals: { source: "portal_index" } %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>

