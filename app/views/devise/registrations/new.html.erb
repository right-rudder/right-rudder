<%= render "shared/navbar_absolute"%>
<main class="bg-zinc-50 h-screen w-full flex flex-col items-center justify-center pb-10">
  <!--Container for contents-->
  <div class="lg:w-3/5 md:w-4/5">
    <h1 class="font-bold text-4xl text-center py-10">Sign up</h1>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |form| %>
      <%# FOR ERRORS %>
      <div class="px-10">
        <% if notice.present? %>
          <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg h-full" id="notice"><%= notice %></p>
        <% end %>
        <% if alert.present? %>
          <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg h-full" id="alert"><%= alert %></p>
        <% end %>
        <%= render "devise/shared/error_messages", resource: resource %>
      </div>

      <!--Container for fields/button-->
      <div class="px-10 pb-10">

        <div class="my-5">
          <%= form.label :Email %>
          <%= form.email_field :email, placeholder: "Email", class: "placeholder-gray-300 block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full focus:border-blue-800 focus:ring-blue-800" %>
        </div>

        <div class="my-5">
          <%= form.label "Password" %>
          <%= form.password_field :password, placeholder: "Password", class: "placeholder-gray-300 block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full focus:border-blue-800 focus:ring-blue-800" %>
        </div>

        <div class="my-5">
          <%= form.label "Confirm Password" %>
          <%= form.password_field :password_confirmation, placeholder: "Password", class: "placeholder-gray-300 block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full focus:border-blue-800 focus:ring-blue-800" %>
        </div>

        <div class="inline">
          <%= form.submit "Sign Up", class: "rounded-lg py-3 px-5 w-full bg-blue-800 text-white inline-block font-medium cursor-pointer hover:bg-blue-700 active:bg-blue-600" %>
        </div>
      </div>
    <% end %>

  </div>
</main>